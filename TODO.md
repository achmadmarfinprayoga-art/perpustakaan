# TODO: Fix White Screen Issue on Siswa Index Page

## Steps:
- [x] 1. Update `resources/views/layout/master.blade.php` with complete `<head>` including `@vite`, Font Awesome CDN, meta tags.
- [ ] 2. Run `npm run dev` to start Vite dev server.
- [ ] 3. Run `php artisan serve` (if not running).
- [ ] 4. Visit http://127.0.0.1:8000/siswa to verify styled page.
- [ ] 5. If no data, check DB/seeds: `php artisan migrate`, `php artisan db:seed`.

Progress: Starting step 1.
